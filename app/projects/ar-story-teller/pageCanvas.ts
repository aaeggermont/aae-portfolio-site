/**
 * Page canvas for `/work/ar-story-teller` only (see `app/(public)/work/ar-story-teller/layout.tsx`).
 * Sync mentally with `PAGE_CANVAS` in `lib/theme/pageCanvas.ts` when tweaking contrast.
 */
export const AR_STORY_TELLER_PAGE_CANVAS = "#ffffff" as const;
